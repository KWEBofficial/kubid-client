import styled from "@emotion/styled";

export const ImagePreviewContainer = styled.div`
  .image-preview {
    max-width: 200px; // 이미지 최대 너비
    height: auto; // 높이 자동 조절로 비율 유지
    margin-top: 10px;
    border: 1px solid #ddd;
    border-radius: 4px;
    padding: 5px;
  }
`;  